# Gba.Core

Your emulator code lives here. Follow the current lesson shown by `learn.cmd`; it prepares missing starter files in the appropriate folder. New `.cs` files are included automatically.

Start with [memory that remembers bytes](../../course/01-ewram/README.md). The [course](../../course/README.md) teaches each component and its required C# together. Architecture reading is optional.
